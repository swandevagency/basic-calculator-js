// ? Arsam code

// * importing the buttons

// * importing the operation buttons

// * importing the clear and all clear buttons

// * define current and previous section in the display

// * write a function to put current number in previous number when the operation is clicked

// * set the operation button so when they are clicked the do what we desire

// * define the equal button so when we click the second time on number for current number its calculated with the previous number

// ? Kourosh code

// * define application input

// 10 numbers, 4 operation, 3 functions

const insertInput = (input) => {
console.log("🚀 ~ file: test.js ~ line 37 ~ insertInput ~ input", input)
    
}

const allClear = () => {
    
}
const clear = () => {
    
}
const calculate = () => {
    
}


insertInput(2);

// * handle functionality






























// * i set output